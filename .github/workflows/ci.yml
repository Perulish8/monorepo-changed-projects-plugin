name: CI

on:
  push:
    branches: [ main, develop, "release-please**" ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        java-version: [17, 21]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Full history for git-based tests

      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java-version }}
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-disabled: true

      - name: Configure Git
        run: |
          git config --global user.email "ci@github.com"
          git config --global user.name "GitHub CI"

      - name: Grant execute permission for gradlew (Unix)
        if: runner.os != 'Windows'
        run: chmod +x gradlew

      - name: Build plugins
        run: ./gradlew :monorepo-build-plugin:build :monorepo-release-plugin:build -x test -x unitTest -x integrationTest -x functionalTest --no-daemon --stacktrace

      - name: Run unit tests
        run: ./gradlew :monorepo-build-plugin:unitTest :monorepo-release-plugin:unitTest --no-daemon --stacktrace

      - name: Run integration tests
        run: ./gradlew :monorepo-release-plugin:integrationTest --no-daemon --stacktrace

      - name: Run functional tests
        run: ./gradlew :monorepo-build-plugin:functionalTest :monorepo-release-plugin:functionalTest --no-daemon --stacktrace

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: test-reports-${{ matrix.os }}-java${{ matrix.java-version }}
          path: |
            monorepo-build-plugin/build/reports/tests/
            monorepo-build-plugin/build/test-results/
            monorepo-release-plugin/build/reports/tests/
            monorepo-release-plugin/build/test-results/
          retention-days: 7

      - name: Upload build artifacts
        if: matrix.os == 'ubuntu-latest' && matrix.java-version == '17'
        uses: actions/upload-artifact@v6
        with:
          name: plugin-jar
          path: |
            monorepo-build-plugin/build/libs/*.jar
            monorepo-release-plugin/build/libs/*.jar
          retention-days: 30

  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Configure Git
        run: |
          git config --global user.email "ci@github.com"
          git config --global user.name "GitHub CI"

      - name: Check code style
        run: ./gradlew :monorepo-build-plugin:check :monorepo-release-plugin:check --no-daemon --stacktrace

      - name: Verify plugin descriptors
        run: ./gradlew :monorepo-build-plugin:validatePlugins :monorepo-release-plugin:validatePlugins --no-daemon --stacktrace

  publish-local:
    name: Test Local Publishing
    runs-on: ubuntu-latest
    needs: [build, code-quality]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Publish to Maven Local
        run: ./gradlew :monorepo-build-plugin:publishToMavenLocal :monorepo-release-plugin:publishToMavenLocal --no-daemon --stacktrace

      - name: Verify published artifacts
        run: |
          if [ -d ~/.m2/repository/io/github/doug-hawley/monorepo-build-plugin ]; then
            echo "✅ monorepo-build-plugin published successfully to Maven Local"
            ls -la ~/.m2/repository/io/github/doug-hawley/monorepo-build-plugin/
          else
            echo "❌ monorepo-build-plugin not found in Maven Local"
            exit 1
          fi
          if [ -d ~/.m2/repository/io/github/doug-hawley/monorepo-release-plugin ]; then
            echo "✅ monorepo-release-plugin published successfully to Maven Local"
            ls -la ~/.m2/repository/io/github/doug-hawley/monorepo-release-plugin/
          else
            echo "❌ monorepo-release-plugin not found in Maven Local"
            exit 1
          fi
